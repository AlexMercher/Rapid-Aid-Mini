# Core Dependencies
requests>=2.31.0          # HTTP client for Ollama API
Pillow>=10.0.0            # Image processing
reportlab>=4.0.0          # PDF generation
python-dateutil>=2.8.0    # Date/time handling

# Optional - for better logging
python-dotenv>=1.0.0      # Environment variable management

# Groq Cloud API (Stage 2 - text JSON extraction)
groq>=0.9.0               # Groq cloud LLM SDK

# Streamlit UI
streamlit>=1.30.0         # Web application framework

# Testing
pytest>=7.0.0             # Test framework

# Phase 7 — Florence-2 semantic narration (replaces bakllava)
# Weights: HuggingFace cache only — NOT in project directory
transformers>=4.38.0,<5.0  # Pin <5.0: Florence-2 remote code incompatible with 5.x
timm>=0.9.0                # Required by Florence-2 vision encoder
einops>=0.7.0              # Required by Florence-2 model code
accelerate>=0.26.0         # Required for device_map='auto' in from_pretrained
